@"
# Transaction Reconciliation

.NET console application that ingests a mocked 24-hour transaction snapshot, reconciles it into SQLite using EF Core, records audits, handles revocations, and supports idempotent reruns.

## Status
Project setup in progress.
"@ | Set-Content README.md